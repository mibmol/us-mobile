import React from 'react';
import { View } from 'react-native';
import { MainMenuHeader } from '../headers/headers';

const PlaylistsView = () => {
	return (
		<View>
			<MainMenuHeader title="Playlists" />
		</View>
	);
};

export default PlaylistsView;
