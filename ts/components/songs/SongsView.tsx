import React from 'react';
import { View } from 'react-native';
import { MainMenuHeader } from '../headers/headers';

const SongsView = () => {
	return (
		<View>
			<MainMenuHeader title="Songs" />
		</View>
	);
};

export default SongsView;
