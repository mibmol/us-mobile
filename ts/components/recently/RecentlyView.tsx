import React from 'react';
import { View } from 'react-native';
import { MainMenuHeader } from '../headers/headers';

const RecentlyView = () => {
	return (
		<View>
			<MainMenuHeader title="Recently added" />
		</View>
	);
};

export default RecentlyView;
