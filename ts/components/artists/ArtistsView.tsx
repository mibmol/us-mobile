import React from 'react';
import { View } from 'react-native';
import { MainMenuHeader } from '../headers/headers';

const ArtistsView = () => {
	return (
		<View>
			<MainMenuHeader title="Artists" />
		</View>
	);
};

export default ArtistsView;
